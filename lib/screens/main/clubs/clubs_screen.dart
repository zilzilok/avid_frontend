import 'package:flutter/material.dart';

class ClubsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Клубы')),
    );
  }
}